import 'package:flutter/material.dart';

class FuelCalculationsMainPage extends StatelessWidget {
  const FuelCalculationsMainPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Fuel"),
      ),
      body: Center(
        child: const Text("Fuel"),
      ),
    );
  }
}
